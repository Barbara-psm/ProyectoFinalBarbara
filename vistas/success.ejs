<!DOCTYPE html>
<html lang="es">

<head>
    <meta http-equiv="refresh" content="6;url=/catalogo"> <!-- Redirige después de 6 segundos -->
    <title><%= title %></title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .invoice-container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
        }

        .invoice-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .invoice-header h1 {
            color: #28a745;
            font-size: 2rem;
        }

        .invoice-details {
            margin-bottom: 20px;
        }

        .invoice-details p {
            margin: 5px 0;
        }

        .invoice-products {
            margin-bottom: 20px;
        }

        .invoice-products table {
            width: 100%;
            border-collapse: collapse;
        }

        .invoice-products th, .invoice-products td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        .invoice-products th {
            background-color: #f2f2f2;
        }

        .invoice-total {
            text-align: right;
            font-size: 1.2rem;
            font-weight: bold;
        }

        .redirect-message {
            text-align: center;
            margin-top: 20px;
            font-size: 1rem;
            color: #666;
        }
    </style>
</head>

<body>
    <div class="invoice-container">
        <div class="invoice-header">
            <h1>¡Pago realizado!</h1>
            <p>Gracias por tu compra. Aquí está el resumen de tu pedido:</p>
        </div>

        <div class="invoice-details">
            <p><strong>Número de factura:</strong> <%= factura %></p>
            <p><strong>ID del pago:</strong> <%= idPago %></p>
            <p><strong>Total:</strong> €<%= total %></p>
        </div>

        <div class="invoice-total">
            <p><strong>Total a pagar:</strong> €<%= total %></p>
        </div>

        <div class="invoice-details">
            <h3>Dirección de envío:</h3>
            <p><%= direccion.recipient_name %></p>
            <p><%= direccion.line1 %></p>
            <p><%= direccion.city %>, <%= direccion.state %> <%= direccion.postal_code %></p>
            <p><%= direccion.country_code %></p>
        </div>

        <div class="redirect-message">
            Serás redirigido al catálogo...
        </div>
    </div>
</body>

</html>